"""Initialize the docker.python package.

This file marks the directory as a Python package and avoids implicit
namespace package warnings (e.g., INP001 from ruff). No initialization
logic is required.
"""
